import buildModule from './builders/buildModule'
import compose from './composers/compose'
import composeActions from './composers/composeActions'
import composeMutations from './composers/composeMutations'
import composeGetters from './composers/composeGetters'

export default {
  buildModule,
  compose,
  composeActions,
  composeGetters,
  composeMutations
}
